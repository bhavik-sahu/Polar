# Supply-chain policy (npm 11+). See README — Developing.
engine-strict=true
# Release-age: npm maps this to an internal `before` cutoff; `npm config get min-release-age` may stay null—check `npm config get before` for the effective timestamp when verifying installs.
min-release-age=5
allow-git=none
